# James Darren Muir Resume

This package build my resume using [RenderCV](https://github.com/rendercv/rendercv).

## Setup

Install dependencies:

```shell
sudo apt install pipx
pipx install 'rendercv[full]'
```

## Build

I've set up a Makefile so you can simply run `make` to build the project.
